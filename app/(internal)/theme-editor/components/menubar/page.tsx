import { PlaceholderComponentPage } from '../placeholder-template';

export default function MenubarComponentPage() {
  return (
    <PlaceholderComponentPage
      componentName="Menubar"
      category="Interactive Component"
      description="A visually persistent menu common in desktop applications that provides a set of actions."
    />
  );
} 